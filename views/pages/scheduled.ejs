<!DOCTYPE html>
<html lang="en">
    <% include ../partials/head %>
    
    <body class="uk-background-muted">
        <h1>Upcoming tours for: </h1>

        <div>
            <% scheduled.forEach(function(schtr) { %>
                <%- include('../partials/scheduledtourcard', {schtr: schtr}); %>
            <% }); %>
        </div>
        <% include ../partials/footer %>
    </body>
</html>