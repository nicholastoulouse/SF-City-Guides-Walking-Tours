<!DOCTYPE html>
<html lang="en">
    <% include ../partials/head %>
    
    <body class="uk-background-muted">
        <% include ../partials/nav %>
        <h1>Tours</h1>

        <div>
            <% tours.forEach(function(tour) { %>
                <%- include('../partials/tourcard', {tour: tour}); %>
            <% }); %>
        </div>
        <% include ../partials/footer %>
    </body>
</html>